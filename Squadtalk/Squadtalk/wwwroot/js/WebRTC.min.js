const lk=LivekitClient;const Room=lk.Room;const RoomEvent=lk.RoomEvent;const ParticipantEvent=lk.ParticipantEvent;const Track=lk.Track;const Source=Track.Source;const room=new Room({adaptiveStream:true,dynacast:true,disconnectOnPageLeave:true,publishDefaults:{simulcast:true,videoSimulcastLayers:[lk.VideoPresets.h216],dtx:true,red:true,forceStereo:false,screenShareEncoding:lk.ScreenSharePresets.h1080fps30.encoding,scalabilityMode:"L3T3"},videoCaptureDefaults:{resolution:lk.VideoPresets.h720.resolution}});room.on(RoomEvent.TrackSubscribed,handleTrackSubscribed).on(RoomEvent.TrackUnsubscribed,handleTrackUnsubscribed).on(RoomEvent.Disconnected,handleDisconnect).on(RoomEvent.LocalTrackPublished,handleLocalTrackPublished).on(RoomEvent.LocalTrackUnpublished,handleLocalTrackUnpublished).on(RoomEvent.ParticipantConnected,participantConnected).on(RoomEvent.ParticipantDisconnected,participantDisconnected).on(RoomEvent.MediaDevicesChanged,handleDevicesChanged).on(RoomEvent.MediaDevicesError,async error=>{const failure=lk.MediaDeviceFailure.getFailure(error)}).on(RoomEvent.TrackMuted,handleTrackMuted).on(RoomEvent.TrackUnmuted,handleTrackUnmuted);let maximizeVideoFrame;let maximizeVideoPlayer;let dotnetObject;let roomToken;let serverAddress;let maximizedTrackSid;let maximizedParticipantIdentity;let cameraFrontFacing=false;let bitrateInterval;let microphoneEnabled;let cameraEnabled;let screenShareEnabled;export async function Init(object,url){if(!object){throw new Error("dotnet object is undefined")}dotnetObject=object;serverAddress=url;room.prepareConnection(serverAddress);GetElements()}export function GetElements(){maximizeVideoFrame=document.getElementById("maximized-video-container");maximizeVideoPlayer=document.getElementById("maximized-video")}export async function Start2(token){roomToken=token;try{await room.connect(serverAddress,roomToken)}catch(e){await dotnetObject.invokeMethodAsync("ErrorCallback","Failed to connect to the room","Unable to connect to the room");return false}try{await room.localParticipant.setMicrophoneEnabled(true);microphoneEnabled=true;const microphones=await Room.getLocalDevices("audioinput");await dotnetObject.invokeMethodAsync("MicrophonesUpdatedCallback",mapMediaDevices(microphones));const cameras=await Room.getLocalDevices("videoinput",false);await dotnetObject.invokeMethodAsync("CamerasUpdatedCallback",mapMediaDevices(cameras))}catch(e){await dotnetObject.invokeMethodAsync("ErrorCallback","Unable to access the microphone","You need to grant access to the microphone in order to let others hear you");return false}bitrateInterval=setInterval(displayBitrate,1e3);const participant=room.localParticipant;participant.on(ParticipantEvent.TrackMuted,pub=>displayParticipant(participant)).on(ParticipantEvent.TrackUnmuted,pub=>displayParticipant(participant)).on(ParticipantEvent.IsSpeakingChanged,isSpeaking=>displayParticipant(participant)).on(ParticipantEvent.ConnectionQualityChanged,connectionQuality=>displayParticipant(participant));await displayParticipant(room.localParticipant);return true}export async function Start(token){roomToken=token;try{await room.connect(serverAddress,roomToken);const publication=await room.localParticipant.setMicrophoneEnabled(true);if(!publication){await dotnetObject.invokeMethodAsync("ErrorCallback","Unable to access the microphone","You need to grant access to the microphone in order to let others hear you")}else{microphoneEnabled=true;const microphones=await Room.getLocalDevices("audioinput");await dotnetObject.invokeMethodAsync("MicrophonesUpdatedCallback",mapMediaDevices(microphones));const cameras=await Room.getLocalDevices("videoinput",false);await dotnetObject.invokeMethodAsync("CamerasUpdatedCallback",mapMediaDevices(cameras))}bitrateInterval=setInterval(displayBitrate,1e3);const participant=room.localParticipant;participant.on(ParticipantEvent.TrackMuted,pub=>displayParticipant(participant)).on(ParticipantEvent.TrackUnmuted,pub=>displayParticipant(participant)).on(ParticipantEvent.IsSpeakingChanged,isSpeaking=>displayParticipant(participant)).on(ParticipantEvent.ConnectionQualityChanged,connectionQuality=>displayParticipant(participant));displayParticipant(room.localParticipant);return true}catch(e){console.error(e);return false}}export async function Stop(){if(bitrateInterval)clearInterval(bitrateInterval);if(maximizedParticipantIdentity)MinimizeVideo();await room.disconnect(true)}export async function ToggleMicrophoneEnabled(){await room.localParticipant.setMicrophoneEnabled(!microphoneEnabled);if(!microphoneEnabled){const microphones=await Room.getLocalDevices("audioinput");await dotnetObject.invokeMethodAsync("MicrophonesUpdatedCallback",mapMediaDevices(microphones))}microphoneEnabled=room.localParticipant.isMicrophoneEnabled;return microphoneEnabled}export async function ToggleCameraEnabled(){await room.localParticipant.setCameraEnabled(!cameraEnabled);if(!cameraEnabled){const cameras=await Room.getLocalDevices("videoinput");await dotnetObject.invokeMethodAsync("CamerasUpdatedCallback",mapMediaDevices(cameras))}cameraEnabled=room.localParticipant.isCameraEnabled;return cameraEnabled}export async function ToggleScreenShareEnabled(){const result=await room.localParticipant.setScreenShareEnabled(!screenShareEnabled);screenShareEnabled=room.localParticipant.isScreenShareEnabled;return screenShareEnabled}export function ShowVideo(participantIdentity,videoMaximized){const participant=room.getParticipantByIdentity(participantIdentity);if(!participant){console.error("Participant is undefined");return}const source=videoMaximized===0?Source.Camera:Source.ScreenShare;const publication=participant.getTrackPublication(source);if(!publication){console.error("Publication is undefined");return}const track=publication.track;maximizedTrackSid=publication.trackSid;maximizedParticipantIdentity=participant.identity;maximizeVideoFrame.style.display="block";publication.track.attach(maximizeVideoPlayer);if(source===Source.Camera){attachVideo(track,participant,"camera");const otherTrack=participant.getTrackPublication(Source.ScreenShare)?.track;detachVideo(otherTrack,participant,"video")}else if(source===Source.ScreenShare){attachVideo(track,participant,"video");const otherTrack=participant.getTrackPublication(Source.Camera)?.track;detachVideo(otherTrack,participant,"camera")}}export function MinimizeVideo(){const participant=room.getParticipantByIdentity(maximizedParticipantIdentity);if(!participant){console.error("Participant is undefined");return}const publication=participant.videoTrackPublications.get(maximizedTrackSid);if(!publication){console.error("publication is undefined");return}maximizedTrackSid=null;maximizedParticipantIdentity=null;maximizeVideoFrame.style.display="none";publication.track.detach(maximizeVideoPlayer)}export function SwapCamera(){const cameraPublication=room?.localParticipant.getTrackPublication(Source.Camera);if(!cameraPublication){return}cameraFrontFacing=!cameraFrontFacing;const options={resolution:lk.VideoPresets.h720.resolution,facingMode:cameraFrontFacing?"user":"environment"};cameraPublication.videoTrack?.restartTrack(options)}export function ChangeVolume(participantIdentity,volume,screenShare){const participant=room.getParticipantByIdentity(participantIdentity);if(participant instanceof lk.RemoteParticipant){const source=screenShare?Source.ScreenShareAudio:Source.Microphone;participant.setVolume(volume/100,source)}}export async function ChangeDevice(kind,id){const mediaDeviceKind=kind===0?"audioinput":"videoinput";if(room){await room.switchActiveDevice(mediaDeviceKind,id)}}const mapMediaDevices=devices=>devices.map(x=>({label:x.label,kind:x.kind,id:x.deviceId}));const mapParticipant=participant=>{let totalBitrate=0;for(const t of participant.trackPublications.values()){if(t.track){totalBitrate+=t.track.currentBitrate}}return{Username:participant.name,Id:participant.identity,Sid:participant.sid,Remote:!participant.isLocal,MicrophoneOn:participant.isMicrophoneEnabled,CameraOn:participant.isCameraEnabled,ScreenShareOn:participant.isScreenShareEnabled,ConnectionQuality:participant.connectionQuality,Bitrate:Math.round(totalBitrate),IsSpeaking:participant.isSpeaking}};function attachVideo(track,participant,prefix){if(!track)return;const container=document.getElementById(`${prefix}-container-${participant.sid}`);const player=document.getElementById(`${prefix}-${participant.sid}`);container.style.display="block";track.attach(player)}function detachVideo(track,participant,prefix){if(!track)return;const container=document.getElementById(`${prefix}-container-${participant.sid}`);const player=document.getElementById(`${prefix}-${participant.sid}`);container.style.display="none";track.detach(player)}function handleTrackSubscribed(track,publication,participant){const camera=participant.isCameraEnabled;const screenShare=participant.isScreenShareEnabled;const both=screenShare&&camera;if(track.source===Source.Microphone){const audioPlayer=document.getElementById(`audio-${participant.sid}`);track.attach(audioPlayer)}else if(track.source===Source.ScreenShareAudio){const screenAudioPlayer=document.getElementById(`screen-audio-${participant.sid}`);track.attach(screenAudioPlayer)}else if(track.source===Source.Camera&&!screenShare||both){attachVideo(track,participant,"camera")}else if(track.source===Source.ScreenShare&&!camera){attachVideo(track,participant,"video")}displayParticipant(participant)}function handleTrackUnsubscribed(track,publication,participant){track.detach();if(track.source===Source.Camera){const cameraContainer=document.getElementById(`camera-container-${participant.sid}`);cameraContainer.style.display="none"}else if(track.source===Source.ScreenShare){const videoContainer=document.getElementById(`video-container-${participant.sid}`);videoContainer.style.display="none"}if(track.sid===maximizedTrackSid){maximizeVideoFrame.style.display="none"}displayParticipant(participant)}function handleLocalTrackPublished(publication){const track=publication.track;const participant=room.localParticipant;const camera=participant.isCameraEnabled;const screenShare=participant.isScreenShareEnabled;const both=screenShare&&camera;if(track.source===Source.Camera&&!screenShare||both){attachVideo(track,participant,"camera")}else if(track.source===Source.ScreenShare&&!camera){attachVideo(track,participant,"video")}displayParticipant(participant)}async function handleLocalTrackUnpublished(publication,participant){const track=publication.track;track.detach();if(track.source===Source.Camera){const cameraContainer=document.getElementById(`camera-container-${participant.sid}`);cameraContainer.style.display="none"}else if(track.source===Source.ScreenShare){const videoContainer=document.getElementById(`video-container-${participant.sid}`);videoContainer.style.display="none"}if(track.sid===maximizedTrackSid){maximizeVideoFrame.style.display="none"}displayParticipant(participant)}function handleTrackUnmuted(publication,participant){const track=publication.track;const camera=participant.isCameraEnabled;const screenShare=participant.isScreenShareEnabled;const both=screenShare&&camera;if(track.source===Source.Camera&&!screenShare||both){const cameraContainer=document.getElementById(`camera-container-${participant.sid}`);cameraContainer.style.display="block"}else if(track.source===Source.ScreenShare&&!camera){const videoContainer=document.getElementById(`video-container-${participant.sid}`);videoContainer.style.display="block"}displayParticipant(participant)}function handleTrackMuted(publication,participant){const track=publication.track;if(track.source===Source.Camera){const cameraContainer=document.getElementById(`camera-container-${participant.sid}`);cameraContainer.style.display="none"}else if(track.source===Source.ScreenShare){const videoContainer=document.getElementById(`video-container-${participant.sid}`);videoContainer.style.display="none"}if(track.sid===maximizedTrackSid){maximizeVideoFrame.style.display="none"}displayParticipant(participant)}function handleDisconnect(reason){const channelId=room.name;dotnetObject.invokeMethodAsync("DisconnectedCallback",reason,channelId)}function participantConnected(participant){console.log("participant",participant.identity,"connected",participant.metadata);console.log("tracks",participant.trackPublications);participant.on(ParticipantEvent.TrackMuted,pub=>displayParticipant(participant)).on(ParticipantEvent.TrackUnmuted,pub=>displayParticipant(participant)).on(ParticipantEvent.IsSpeakingChanged,isSpeaking=>displayParticipant(participant)).on(ParticipantEvent.ConnectionQualityChanged,connectionQuality=>displayParticipant(participant));const channelId=room.name;dotnetObject.invokeMethodAsync("ParticipantConnectedCallback",mapParticipant(participant),channelId)}function participantDisconnected(participant){const channelId=room.name;console.log("participant",participant.identity,"disconnected");dotnetObject.invokeMethodAsync("ParticipantDisconnectedCallback",mapParticipant(participant),channelId)}async function handleDevicesChanged(){const allDevices=await Room.getLocalDevices(null,false);allDevices.forEach(x=>console.log("device",x.label,x.kind))}async function displayParticipant(participant){const channelId=room.name;await dotnetObject.invokeMethodAsync("DisplayParticipantCallback",mapParticipant(participant),channelId)}async function displayBitrate(){if(!room||room.state!==lk.ConnectionState.Connected){return}const participants=[...room.remoteParticipants.values()];participants.push(room.localParticipant);for(const participant of participants){let totalBitrate=0;for(const t of participant.trackPublications.values()){if(t.track){totalBitrate+=t.track.currentBitrate}}}}